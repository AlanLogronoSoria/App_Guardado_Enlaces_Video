name: inventario_video_app
description: "Repositorio inteligente de enlaces de videos de redes sociales."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1

  drift: ^2.25.1
  sqlite3_flutter_libs: ^0.5.32
  path_provider: ^2.1.5
  path: ^1.9.1

  receive_sharing_intent: ^1.8.1

  url_launcher: ^6.3.1
  http: ^1.3.0
  html: ^0.15.5
  cached_network_image: ^3.4.1
  intl: ^0.20.2
  uuid: ^4.5.1
  supabase_flutter: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.25.1
  build_runner: ^2.4.15

flutter:
  uses-material-design: true

  assets:
    - assets/
